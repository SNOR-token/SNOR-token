// If you need to create an ISO, set it to true; else it’s ESD installer
// In 'ISO' mode, the stub will skip 'Download', 'Downloading' pages automatically even if these pages in installation sequence.
var Feature_IsISO = false;

// Set the initialize page show time in milli-seconds. 0 to not show the page
var Feature_ShowPageTime = 0;

//true means billboards is enabled
var Feature_IsBillBoardsEnabled = false;
// billboards image switch interval
var Feature_BillboardSwitchInterval = 2000;